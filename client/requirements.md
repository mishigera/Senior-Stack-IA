## Packages
date-fns | Formatting dates for audit logs and chat
recharts | Dashboard charts and analytics
framer-motion | Page transitions and animations
clsx | Class merging utility
tailwind-merge | Class merging utility

## Notes
The application relies on Replit Auth (`/api/login`, `/api/logout`, `/api/auth/user`).
Chat functionality utilizes the provided Replit AI integrations at `/api/conversations` endpoints.
